# ملاحظات مشروع Mandaly

دليل مرجعي كامل للمشروع — لمن لم يرَ الكود من قبل (شخص جديد، أو مساعد ذكاء اصطناعي آخر). اقرأه قبل أي تعديل.

---

## 1. البنية

**التقنيات:** Vite + TypeScript، بناءً كتطبيق PWA (عبر `vite-plugin-pwa`) — أي تطبيق ويب يعمل كتطبيق حقيقي بعد "التثبيت" على الهاتف (أيقونة، شاشة كاملة، عمل بدون إنترنت بعد أول تحميل). بدون أي إطار عمل (framework) مثل React أو Vue — DOM يُبنى ويُحدَّث يدويًا عبر `document.createElement`/`innerHTML`.

النشر تلقائي بالكامل: أي دفع (push) لفرع `claude/zen-sagan-6bshfr` أو `main` يُشغّل `.github/workflows/deploy.yml` الذي يبني المشروع وينشره على GitHub Pages — لا خطوة يدوية لنشر التطبيق نفسه (الـWorker مختلف، انظر القسم 5).

### أهم الملفات ودور كل منها

**البيانات (`src/data/`):**
- `categories.ts` — الأصناف الثلاثة وأيقونة SVG لكل صنف.
- `deck.ts` — بنك الكروت الكامل (199 كرتًا) + تعريف `Card` (انظر القسم 2).
- `session.ts` — كل منطق اختيار وترتيب كروت الجلسة حسب الطول المختار (انظر القسم 4). هذا الملف **لا يعتمد على الـDOM إطلاقًا** (دوال خالصة/pure functions) — لهذا أمكن اختباره بمحاكاة مباشرة (انظر القسم 6).

**اللعب:**
- `game.ts` — الأكبر؛ يدير سحب الكروت، شريط الأدوار، النقاط، حجم خط الكرت التلقائي (shrink-to-fit)، المؤقّت لكل كرت، قائمة "⋮" لطول اللعبة.
- `players.ts` — حالة اللاعبين واللعب (أسماء، نقاط، الدور الحالي) — بالذاكرة فقط، **لا يُحفظ في localStorage إطلاقًا** (كل شيء يبدأ من الصفر عند فتح الصفحة).
- `playerSetup.ts` — شاشة تسجيل أسماء اللاعبين (أو تخطّيها للعب الكلاسيكي بدون أسماء).
- `farewell.ts` — صفحة النتائج الختامية بعد انتهاء الجلسة (الترتيب + الميداليات).
- `timer.ts` — المؤقّت المرئي لكروت الوقت المحدد.

**الواجهة والتقديم:**
- `main.ts` — نقطة الدخول؛ يبني كل الـHTML (كقالب نصي واحد ضخم)، ويربط كل الوحدات ببعضها.
- `intro.ts` — شاشات الترحيب/القوانين، والتنقل بينها (بما فيها إصلاح تداخل الصفحات القديم عبر `showOnlyPage`).
- `install.ts` — زر "ثبّت التطبيق" (انظر ملاحظة أدناه).
- `font.ts` — تبديل خط نص الكروت.
- `share.ts` — مشاركة نص الكرت (مشاركة نظام التشغيل، أو نسخ كبديل).
- `haptics.ts` — اهتزاز خفيف عند السحب/القلب (حيث يدعمه الجهاز).

**الصوت:**
- `sound.ts` — كل شيء صوتي: مؤثرات (Web Audio API، نغمات مُولَّدة برمجيًا + صوت قلب الكرت المسجَّل)، الموسيقى الخلفية (حلقات بلا فجوة، تتوقف تلقائيًا عند مغادرة التبويب)، وحالتا الكتم (مؤثرات/موسيقى) منفصلتان تمامًا.
- `soundMenu.ts` — قائمة إعدادات الصوت (زر 🔊 أعلى الشاشة).

**التفعيل:**
- `activation.ts` — بوابة كود التفعيل (انظر القسم 5).
- `config.ts` — رابط الـWorker فقط (`ACTIVATION_API_URL`).

### أدوات مطوّر (`scripts/`)
- `gen-codes.mjs` — يولّد أكواد تفعيل جديدة.
- `simulate-sessions.mjs` — محاكاة جلسات للتحقق من منطق `session.ts` (انظر القسم 6).
- `gen-icons.mjs` — يولّد أيقونات PWA من `icon-bg.svg`.

---

## 2. بنك الكروت

**الأصناف الثلاثة (الاسم التجاري = اسم العرض في التطبيق، لا فرق بينهما):**

| الصنف | العدد | الوصف |
|---|---|---|
| قلوب مفتوحة | 61 | حديث واستماع، بلا نقاط، بلا فوز أو خسارة |
| حلبة العائلة | 129 | تخمين وتحدٍّ، بنقاط فردية |
| اقلب الطاولة | 9 | يغيّر قواعد الكرت التالي، مفاجأة خفيفة |
| **المجموع** | **199** | |

من أصل الـ129 في "حلبة العائلة"، **44 كرتًا** موسومة `mechanic: 'xy'` — قالب "اختر لاعبًا واحدًا كهدف، امنح نقطة لمن يطابق توقعًا عن اختياره/إجابته" (مثال: *"اختر لاعبًا: يصل مبكرًا، في الموعد، أم متأخرًا؟ (تطابق = +1)"*). هذا الوسم يُستخدم في منطق تكوين الجلسة (القسم 4) لأن هذا القالب هو الأكثر تكرارًا في البنك ويحتاج سقفًا حتى لا يهيمن على الجلسة.

### تعريف الكرت (في `deck.ts`)

```ts
export interface Card {
  cat: CardCategory;        // الصنف — أحد الثلاثة أعلاه
  text: string;              // نص الكرت الكامل، يحمل قواعده/نقاطه داخله
  duration?: number;         // ثوانٍ، فقط إذا كان للكرت مؤقّت مضمّن
  needs3?: boolean;          // يُستبعد تلقائيًا إذا كان المسجَّلون أقل من 3 لاعبين
  mechanic?: 'xy';           // فقط للكروت من قالب "اختر لاعبًا...تطابق=+1"
}
```

### القوانين الأربعة لأي كرت جديد

1. **كل كرت يحمل نقاطه صراحة في نصه** — لا نقاط ضمنية أو "حسب تقدير اللاعبين" بلا توضيح؛ الصياغة نفسها تقول "= +1" أو ما يعادلها.
2. **يعمل بلاعبَين كحد أدنى** — وإلا يُوسم `needs3: true` (مثال: كرت "من يجلس أين؟" الذي يحتاج تجمّعًا حقيقيًا حول الشخص).
3. **بلا افتراض ترتيب جلوس** — ممنوع "الشخص على يمينك" أو "من يجلس مقابلك"؛ التطبيق لا يعرف كيف يجلس اللاعبون فعليًا. استخدم "اختر لاعبًا" بدل ذلك.
4. **"اقلب الطاولة" نسبة ثابتة تقريبًا 5%** من أي جلسة (انظر `FLIP_RATIO` في `session.ts`) — فئة صغيرة ومركّزة عمدًا؛ لا تضف كروتًا كثيرة لهذا الصنف دفعة واحدة دون إعادة حساب هذه النسبة.

### خطوات إضافة كرت جديد

1. افتح `src/data/deck.ts`.
2. أضف كائنًا جديدًا داخل مصفوفة `DECK`، بنفس الصيغة:
   ```ts
   { cat: 'حلبة العائلة', text: '...' },
   ```
3. إذا كان الكرت يطابق قالب xy (اختيار لاعب واحد + تطابق توقّع = نقطة)، أضف `mechanic: 'xy'`. **كيف تعرف أنه xy:** اسأل نفسك — هل الآلية هي "اختر لاعبًا واحدًا كهدف، ثم امنح نقطة لمن خمّن/طابق شيئًا عنه"؟ إذا كان الهدف أكثر من لاعب واحد (اختر لاعبَين)، أو كانت الآلية تخمين جماعي عن "مين في العائلة..." (بلا اختيار هدف واحد أولاً)، أو كان مجرد تحدٍّ/أداء بلا تخمين — فهو **ليس** xy.
4. إذا كان الكرت يحتاج 3 لاعبين فعليًا، أضف `needs3: true`.
5. إذا كان نص الكرت يذكر مدة زمنية محددة (كـ "ثلاثون ثانية")، أضف `duration: 30` (بالثواني) ليظهر المؤقّت تلقائيًا.
6. شغّل `npx tsc --noEmit` للتأكد من عدم وجود خطأ صياغة.
7. إذا أضفت عدة كروت أو غيّرت وسم `mechanic`/`needs3` على نطاق واسع، شغّل `npm run simulate` (القسم 6) للتأكد أن الجلسات ما زالت تُبنى بلا خروقات.

---

## 3. منطق الجلسة (`src/data/session.ts`)

### أطوال اللعبة

| الطول | العدد الاسمي |
|---|---|
| قصيرة جدًا | 30 |
| قصيرة | 70 |
| متوسطة | 150 |
| طويلة | كل الكروت المتاحة فعليًا (وليس رقمًا ثابتًا) |

"المتاح" = بعد استبعاد الكروت الموسومة `needs3: true` إذا كان عدد اللاعبين المسجَّلين أقل من 3 (يشمل وضع اللعب الكلاسيكي بلا أسماء، حيث العدد يُعامَل كـ0). إذا كان الطول المطلوب أكبر من المتاح، يُستخدم كل المتاح.

### أولويات تكوين الجلسة (اختيار الكروت)

دالة `selectSessionCards()`:
1. **اقلب الطاولة** — تقريبًا 5% من طول الجلسة، بحد أقصى العدد المتوفر (9).
2. **كروت xy** — سقف 25% من الجلسة القصيرة جدًا أو القصيرة، 40% من المتوسطة؛ **بلا سقف في الطويلة** (تدخل كلها). إذا لم تكفِ بقية الكروت لملء الجلسة، يرتفع السقف تلقائيًا بالقدر اللازم فقط.
3. **الباقي** (قلوب مفتوحة + حلبة العائلة غير-xy) — يُوزَّع بحيث تبقى "قلوب مفتوحة" قريبة من ثلث الجلسة قدر الإمكان.
4. الاختيار عشوائي داخل كل مجموعة (لهذا تختلف كروت xy من جلسة لأخرى بنفس الطول).

### قواعد الترتيب داخل الجلسة

دالة `orderSessionCards()`:
- لا أكثر من **3 كروت متتالية من نفس الصنف**.
- لا أكثر من **2 كرت xy متتالي**.
- كرتا "اقلب الطاولة" لا يتجاوران أبدًا، ولا يكون "اقلب الطاولة" آخر كرت في الجلسة (لأنه يعدّل قواعد الكرت **التالي**)، ويُفضَّل ألا يظهر ضمن أول 5 كروت.
- الترتيب يُحسب **مرة واحدة** عند بداية الجلسة ويُحفظ في الذاكرة، فأزرار "السابق/التالي" تسيران على نفس التسلسل الثابت.

### لماذا خوارزمية "حفظ المورد النادر"، وماذا فشل قبلها

هذا الجزء استغرق عدة محاولات فاشلة قبل الوصول لحل صحيح — موثَّق هنا حتى لا يُعاد نفس الخطأ لاحقًا:

1. **"اختر الأكبر عددًا دائمًا"** — كانت تُحشر كروت اقلب الطاولة (القليلة العدد) في آخر الجلسة بشكل متكرر، مخالفةً قاعدة "ليس آخر كرت".
2. **عشوائية موزونة بالعدد المتبقي (weighted random)** — أفضل توزيعًا، لكنها لا تضمن نفاد كل المجموعات في نفس الوقت تقريبًا؛ بالصدفة كانت "قلوب مفتوحة" (المجموعة الوحيدة القادرة على "قطع" تسلسل حلبة العائلة) تنفد أحيانًا مبكرًا، فيُضطر لاحقًا لتكرار حلبة العائلة أكثر من الحد المسموح.
3. **"عدالة صارمة" (اختيار الأكثر تأخرًا عن نصيبه دائمًا)** — صحيحة رياضيًا لكنها أنتجت نمطًا آليًا متكررًا يُمل (مثل "قلب-تحدي-تخمين" بنفس الترتيب مرارًا).

**الحل النهائي:** يُحسب لكل مجموعتين متنافستين (مثلاً "قلوب مفتوحة" مقابل "حلبة العائلة") الحد الأدنى الذي يحتاجه كل طرف ليبقى قادرًا على "قطع" تسلسل الطرف الآخر مستقبلاً. طالما كلا الطرفين لديه فائض فوق هذا الحد الأدنى، الاختيار عشوائي حقيقي (تنوع طبيعي). بمجرد أن يقترب طرف من حده الأدنى، **يُحفظ** (تُفضَّل المجموعة الأخرى) حتى يفرضه القيد الصارم عند الحاجة الفعلية فقط — لا قبل ولا بعد.

---

## 4. نظام التفعيل

### كيف يعمل

- صيغة الكود: `MDLY-XXXX-XXXX` — 7 أحرف عشوائية من أبجدية بلا رموز ملتبسة (بلا 0/O، 1/I/L) + حرف تحقّق (checksum) واحد يُحسب من الأحرف السبعة. هذا يسمح برفض كود مكتوب خطأ **محليًا** (بدون اتصال) قبل أي طلب للخادم.
- عند إدخال كود صحيح الشكل، يُرسَل طلب واحد لمرة واحدة فقط إلى الـWorker مع معرّف جهاز عشوائي (`crypto.randomUUID()`، يُحفظ محليًا).
- الخادم (Cloudflare KV) يسمح بحد أقصى **جهازين** لكل كود. نفس الجهاز يعيد المحاولة بأمان (لا يستهلك مقعدًا إضافيًا).
- بعد نجاح التفعيل مرة واحدة، لا يُعاد الاتصال بالخادم إطلاقًا على ذلك الجهاز — التطبيق يعمل بالكامل دون إنترنت بعدها.

### مفاتيح localStorage — **لا تُلمَس أبدًا** (يخص التفعيل)

- `mandaly-activated` — `'1'` إذا فُعِّل الجهاز.
- `mandaly-device-id` — معرّف الجهاز العشوائي.
- `mandaly-activation-code` — الكود المستخدَم في التفعيل.

(مفاتيح أخرى غير حساسة، آمن تعديلها: `mandaly-font`, `mandaly-intro-seen`, `mandaly-muted`, `mandaly-music-enabled` — تفضيلات واجهة فقط.)

### رابط الـWorker الحالي

`https://mandaly-activation.mouhamedali-mandaly.workers.dev` (مُعرَّف في `src/config.ts`).

### توليد ورفع أكواد جديدة (بدون كتابة أي كود فعلي هنا)

```bash
node scripts/gen-codes.mjs 200
```
يولّد ملفين محليين (git-ignored، لا تُرفعا أبدًا لأي مستودع): ملف CSV لسجلاتك الخاصة، وملف JSON بصيغة Wrangler. ثم:
```bash
cd worker
npx wrangler kv bulk put ../codes-batch-N-seed.json --namespace-id=<المعرّف من wrangler.toml>
```

للتحقق من استخدام الأكواد الحالية:
```
https://mandaly-activation.mouhamedali-mandaly.workers.dev/admin/export?token=<ADMIN_TOKEN الخاص بك>
```
يُنزّل CSV يفتح مباشرة في Excel.

---

## 5. النشر

### التطبيق (GitHub Pages) — تلقائي بالكامل

```bash
git add -A
git commit -m "..."
git push origin claude/zen-sagan-6bshfr
```
هذا وحده كافٍ — GitHub Actions يبني وينشر تلقائيًا خلال دقيقة تقريبًا. تحقّق من النجاح في تبويب Actions على GitHub.

### الـWorker (Cloudflare) — يدوي، فقط عند تعديل `worker/src/index.js`

```bash
cd worker
npx wrangler deploy
```
(يتطلب تسجيل دخول مسبق عبر `npx wrangler login`، مرة واحدة فقط لكل جهاز تُدير منه الـWorker.)

---

## 6. اختبار المحاكاة

بعد أي تعديل على `src/data/deck.ts` (كروت جديدة، تغيير وسم `mechanic`/`needs3`) أو `src/data/session.ts`، شغّل:

```bash
npm run simulate
```

يبني 1000 جلسة وهمية لكل تركيبة من (قصيرة جدًا/قصيرة/متوسطة/طويلة) × (2/3/5 لاعبين) — 12 حالة، 12000 جلسة إجمالًا — ويطبع جدولًا بالنسب الفعلية وعدد خروقات القواعد. **يجب أن تكون الخروقات صفرًا دائمًا.** إن ظهر أي رقم غير صفري، راجع منطق `orderSessionCards`/`selectSessionCards` قبل النشر.

---

## 7. أفكار مؤجلة

هذه أفكار طُرحت أثناء العمل على المشروع ولم تُنفَّذ بعد — مذكورة هنا كتذكير فقط، بلا تفاصيل تنفيذ جاهزة:

- **أدوار ليلية** — نمط لعب مختلف للجلسات المسائية/الهادئة.
- **لعبة لوتاريا تونسية** — إضافة نمط لعب مستوحى من اللوتو التونسي التقليدي.
- **ترجمة التطبيق** — دعم لغات أخرى غير العربية.
- **ملف APK** — تحويل الـPWA لملف APK قابل للتثبيت المباشر على أندرويد عبر PWA Builder (https://www.pwabuilder.com).
- **ملفات الأسئلة G-01 إلى G-04** — مراجع خارجية (خارج هذا المستودع حاليًا) لدفعات أسئلة إضافية محتملة.

---

## 8. ميزة مؤجلة: رحلة الكنز

نوقشت بالتفصيل (تصميم كامل + مراجعة معمارية) لكن **لم تُنفَّذ بعد عمدًا** — القرار: تجربة التطبيق أولًا مع عائلة حقيقية، ثم البناء لاحقًا. هذا القسم يوثّق القرارات النهائية المتفق عليها حتى لا تُعاد المناقشة من الصفر.

### الفكرة

وضع تعاوني اختياري: العائلة كلها تحرّك قطعة واحدة على مسار نحو "كنز". لا يوجد فائز ضد خاسر — العائلة معًا ضد اللعبة.

### القرارات النهائية

- **التفعيل:** عنصر جديد داخل قائمة "⋮" (بجانب "قلوب مفتوحة فقط" الحالي)، **يُقفل بعد سحب أول كرت في الجلسة** (لا يمكن تفعيله/تعطيله منتصف الجلسة).
- **التقدّم — حسب صنف الكرت فقط، بلا استثناءات:**
  - قلوب مفتوحة = خطوتان
  - حلبة العائلة = خطوة واحدة
  - اقلب الطاولة = خطوة واحدة (تأثيرها النصي يبقى كما هو تمامًا — يطبّقه اللاعبون يدويًا كأي كرت آخر، **بلا أي حدث آلي إضافي** يحرّك المسار بشكل مفاجئ)
- **لا علاقة إطلاقًا بين تقدّم المسار ومنح النقاط الفردية** — قرار متعمّد: منح النقاط حاليًا بلا أي تحقق (أي لاعب يضغط شريحة أي لاعب في أي لحظة)، وربط مورد تعاوني مشترك بآلية قابلة للتلاعب يفتح ثغرة ثقة داخل العائلة نفسها.
- **المفاجآت (رجوع خطوتين / قفزة 3 / مضاعفة الخطوة القادمة):** تكون **خانات مرئية ثابتة على المسار نفسه** (يراها اللاعبون مسبقًا عند فتح الخريطة)، وليست قرارًا عشوائيًا خفيًا يُطبَّق وقت السحب — مع إشعار واضح عند الوصول فعليًا إلى إحداها.
- **3 محطات** عند ربع/نصف/ثلاثة أرباع المسار: تعرض كرت "قلوب مفتوحة" من البنك الحالي (لا كروت جديدة مخصّصة) بتأطير بصري خاص "صندوق كنز". لاحقًا: احتمال استخدام أسئلة G-02 (بعد مراجعتها — انظر القسم 7) بدل كروت قلوب مفتوحة العادية.
- **طول المسار:** يُضبط عبر `scripts/simulate-sessions.mjs` نفسه (يُوسَّع ليحسب مجموع الخطوات، لا كرت وهمي جديد)، بحيث تصل العائلة للكنز في نحو **ثلثي الجلسات**، لكل طول من الأطوال الأربعة على حدة.
- **وضع "قلوب مفتوحة فقط":** قرار مفتوح غير محسوم بعد — إما تعطيل رحلة الكنز بالكامل في هذا الوضع، أو حساب طول مسار خاص به (بما أن كل الكروت من نفس الصنف = خطوتان لكل كرت، حساب مختلف عن الوضع العادي).

### الملفات المتوقّع تأثّرها عند التنفيذ

- **جديد:** `src/treasure.ts` — وحدة حالة بسيطة في الذاكرة (بلا تخزين)، بنفس نمط `players.ts`/`sound.ts` الحالي.
- `src/game.ts` — ثلاث نقاط ربط فقط: داخل `drawCard()` (زيادة الخطوات حسب `card.cat`)، داخل معالج ضغط `turnPlayers` (لا شيء يُضاف هنا فعليًا، بما أن القرار فصل النقاط عن التقدّم)، وداخل `notifySessionChanged()` (تصفير المسار عند إعادة/بدء الجلسة).
- `src/main.ts` — markup لشريط تقدّم صغير فوق الكرت + خريطة كاملة تُفتح بالضغط عليه.
- `src/style.css` — تنسيق الشريط والخريطة ومحطات "صندوق الكنز".

### تحذير معماري واحد يجب تذكّره عند التنفيذ

هذه أول ميزة تجعل الكود "يُنفّذ" أثر كرت تلقائيًا (تحريك المسار). كل تأثيرات الكروت حاليًا نصّية بحتة يطبّقها البشر يدويًا — لا يوجد أي "محرك تنفيذ" في الكود. حتى مع فصل التقدّم عن النقاط، حركة المسار نفسها تحتاج إشعارًا بصريًا واضحًا في نفس لحظة السحب (خصوصًا عند المفاجآت) حتى لا تبدو كخلل غامض.

---

*آخر تحديث: توثيق تصميم "رحلة الكنز" المؤجّلة + إضافة طول الجلسة "قصيرة جدًا" (سبتمبر 2026).*
