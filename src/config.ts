// Base URL of the Cloudflare Worker that verifies activation codes (see
// worker/README.md for how to deploy it). Replace the placeholder below
// with your deployed worker's URL, then rebuild and push to redeploy.
export const ACTIVATION_API_URL = 'https://REPLACE-ME.workers.dev';
